"use client";

import React from "react";

type RankingClassificationSectionProps = {
  children: React.ReactNode;
};

export default function RankingClassificationSection({
  children,
}: RankingClassificationSectionProps) {
  return <>{children}</>;
}
